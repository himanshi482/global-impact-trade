--- a/app/page.jsx
+++ b/app/page.jsx
@@ -1,5 +1,29 @@
+"use client";
+import Link from 'next/link';

 export default function Home() {
+  const routes = [
+    { label: 'Buyer Discovery', href: '/buyer-discovery', bg: 'bg-blue-600 hover:bg-blue-700' },
+    { label: 'Supplier Discovery', href: '/supplier-discovery', bg: 'bg-indigo-600 hover:bg-indigo-700' },
+    { label: 'My Leads', href: '/my-leads', bg: 'bg-emerald-600 hover:bg-emerald-700' },
+    { label: 'Admin Leads', href: '/admin/leads', bg: 'bg-purple-600 hover:bg-purple-700' },
+  ];

   return (
-    <main className="flex min-h-screen flex-col items-center justify-between p-24">
-      <h1>Global Impact Trade</h1>
+    <main className="min-h-screen bg-gray-100 flex flex-col items-center justify-center p-6">
+      <div className="bg-white p-8 rounded-xl shadow-lg w-full max-w-md text-center">
+        <h1 className="text-2xl font-bold text-gray-800 mb-2">Global Impact Trade</h1>
+        <p className="text-sm text-gray-500 mb-6">Select a section to open</p>
+        <div className="flex flex-col gap-3">
+          {routes.map((route) => (
+            <Link 
+              key={route.href} 
+              href={route.href}
+              className={`w-full py-3 px-4 text-white font-medium rounded-lg shadow transition duration-150 ${route.bg}`}
+            >
+              {route.label}
+            </Link>
+          ))}
+        </div>
+      </div>
     </main>
   );
 }