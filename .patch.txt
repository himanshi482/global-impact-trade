diff --git a/app/admin/hs-codes/page.js b/app/admin/hs-codes/page.js
new file mode 100644
index 0000000..26fc46b
--- /dev/null
+++ b/app/admin/hs-codes/page.js
@@ -0,0 +1,302 @@
+"use client";
+
+import { useState, useEffect } from "react";
+import Link from "next/link";
+import AuthGuard from "../../../components/AuthGuard";
+import FieldLabel from "../../../components/FieldLabel";
+
+const emptyForm = {
+  code: "",
+  description: "",
+  chapter: "",
+  bcd: "",
+  sws: "",
+  igst: "",
+  country: "India",
+};
+
+export default function AdminHsCodesPage() {
+  const [codes, setCodes] = useState([]);
+  const [loading, setLoading] = useState(true);
+  const [error, setError] = useState("");
+  const [showModal, setShowModal] = useState(false);
+  const [editingId, setEditingId] = useState(null);
+  const [form, setForm] = useState(emptyForm);
+
+  useEffect(() => {
+    loadCodes();
+  }, []);
+
+  async function loadCodes() {
+    try {
+      const res = await fetch("/api/admin/hs-codes", { credentials: "include" });
+      const data = await res.json();
+      if (res.ok) setCodes(data.data || []);
+      else setError(data.error || "Failed to load HS codes");
+    } catch (err) {
+      console.error(err);
+      setError("Failed to fetch HS code records");
+    } finally {
+      setLoading(false);
+    }
+  }
+
+  const openCreate = () => {
+    setEditingId(null);
+    setForm(emptyForm);
+    setShowModal(true);
+  };
+
+  const openEdit = (c) => {
+    setEditingId(c.id);
+    setForm({
+      code: c.code || "",
+      description: c.description || "",
+      chapter: c.chapter || "",
+      bcd: c.bcd || "",
+      sws: c.sws || "",
+      igst: c.igst || "",
+      country: c.country || "India",
+    });
+    setShowModal(true);
+  };
+
+  const handleSubmit = async (e) => {
+    e.preventDefault();
+    try {
+      const url = editingId ? `/api/admin/hs-codes/${editingId}` : "/api/admin/hs-codes";
+      const method = editingId ? "PUT" : "POST";
+      const res = await fetch(url, {
+        method,
+        headers: { "Content-Type": "application/json" },
+        credentials: "include",
+        body: JSON.stringify(form),
+      });
+      if (res.ok) {
+        setShowModal(false);
+        setForm(emptyForm);
+        setEditingId(null);
+        loadCodes();
+      } else {
+        const data = await res.json().catch(() => ({}));
+        alert(data.error || "Failed to save HS code");
+      }
+    } catch (err) {
+      console.error("Save HS code error", err);
+    }
+  };
+
+  const handleDelete = async (id) => {
+    if (!confirm("Are you sure you want to delete this HS code record?")) return;
+    try {
+      const res = await fetch(`/api/admin/hs-codes/${id}`, {
+        method: "DELETE",
+        credentials: "include",
+      });
+      if (res.ok) {
+        setCodes((prev) => prev.filter((c) => c.id !== id));
+      }
+    } catch (err) {
+      console.error("Delete HS code error", err);
+    }
+  };
+
+  return (
+    <AuthGuard>
+      <main className="min-h-screen bg-[var(--ink)] py-12 px-6">
+        <div className="mx-auto max-w-6xl">
+          <div className="mb-6">
+            <Link href="/admin" className="font-mono text-xs text-[var(--brass)] hover:underline">
+              ← Back to Admin Console
+            </Link>
+          </div>
+
+          <FieldLabel>Tariff Management</FieldLabel>
+          <div className="flex flex-col md:flex-row md:items-center justify-between gap-4 border-b border-[var(--brass)]/20 pb-6 mb-8">
+            <div>
+              <h1 className="font-display text-3xl md:text-4xl text-[var(--paper)]">
+                HS Code Directory CRUD
+              </h1>
+              <p className="mt-1 text-xs text-[var(--muted)] font-mono">
+                Add, edit, or remove HS classification codes and their duty rates.
+              </p>
+            </div>
+            <button
+              onClick={openCreate}
+              className="rounded bg-[var(--brass)] px-5 py-2.5 font-mono text-xs font-bold uppercase text-[var(--ink)] hover:brightness-110"
+            >
+              + Add New HS Code
+            </button>
+          </div>
+
+          {loading ? (
+            <div className="flex justify-center py-20 font-mono text-xs text-[var(--brass)]">
+              <span className="h-4 w-4 rounded-full border-2 border-[var(--brass)] border-t-transparent animate-spin mr-2"></span>
+              Loading HS codes...
+            </div>
+          ) : error ? (
+            <div className="rounded-xl border border-red-500/30 bg-red-500/10 p-6 text-center text-sm text-red-300 font-mono">
+              {error}
+            </div>
+          ) : (
+            <div className="rounded-xl border border-[var(--brass)]/30 bg-[var(--ink-2)] overflow-hidden shadow-2xl font-mono text-xs">
+              <div className="overflow-x-auto">
+                <table className="w-full text-left border-collapse">
+                  <thead>
+                    <tr className="border-b border-[var(--brass)]/20 bg-[var(--ink)] text-[var(--brass)] uppercase text-[10px] tracking-wider">
+                      <th className="p-4">ID</th>
+                      <th className="p-4">Code</th>
+                      <th className="p-4">Description</th>
+                      <th className="p-4">Chapter</th>
+                      <th className="p-4">BCD / SWS / IGST</th>
+                      <th className="p-4">Country</th>
+                      <th className="p-4 text-right">Actions</th>
+                    </tr>
+                  </thead>
+                  <tbody className="divide-y divide-[var(--brass)]/10 text-[var(--paper)]">
+                    {codes.length === 0 ? (
+                      <tr>
+                        <td colSpan={7} className="p-6 text-center text-[var(--muted)]">
+                          No HS codes yet.
+                        </td>
+                      </tr>
+                    ) : (
+                      codes.map((c) => (
+                        <tr key={c.id} className="hover:bg-[var(--brass)]/5 transition">
+                          <td className="p-4 font-bold text-[var(--brass)]">#{c.id}</td>
+                          <td className="p-4 font-bold">{c.code}</td>
+                          <td className="p-4 max-w-xs truncate">{c.description}</td>
+                          <td className="p-4 text-[var(--muted)]">Ch. {c.chapter}</td>
+                          <td className="p-4 text-[var(--muted)]">
+                            {c.bcd || "-"} / {c.sws || "-"} / {c.igst || "-"}
+                          </td>
+                          <td className="p-4 text-[var(--muted)]">{c.country}</td>
+                          <td className="p-4 text-right space-x-2 whitespace-nowrap">
+                            <button
+                              onClick={() => openEdit(c)}
+                              className="rounded border border-[var(--brass)]/40 px-3 py-1 text-[10px] text-[var(--brass)] hover:bg-[var(--brass)]/10 font-bold"
+                            >
+                              Edit
+                            </button>
+                            <button
+                              onClick={() => handleDelete(c.id)}
+                              className="rounded border border-red-500/40 px-3 py-1 text-[10px] text-red-400 hover:bg-red-950 font-bold"
+                            >
+                              Delete
+                            </button>
+                          </td>
+                        </tr>
+                      ))
+                    )}
+                  </tbody>
+                </table>
+              </div>
+            </div>
+          )}
+        </div>
+
+        {/* Modal */}
+        {showModal && (
+          <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/80 p-4 font-mono text-xs">
+            <div className="w-full max-w-md rounded-xl border border-[var(--brass)]/40 bg-[var(--ink-2)] p-6 shadow-2xl space-y-4">
+              <h3 className="font-display text-xl text-[var(--paper)]">
+                {editingId ? "Edit HS Code" : "Add HS Code"}
+              </h3>
+              <form onSubmit={handleSubmit} className="space-y-3">
+                <div className="grid grid-cols-2 gap-3">
+                  <div>
+                    <label className="block text-[var(--muted)] uppercase mb-1">Code *</label>
+                    <input
+                      type="text"
+                      required
+                      value={form.code}
+                      onChange={(e) => setForm({ ...form, code: e.target.value })}
+                      className="w-full rounded border border-[var(--brass)]/30 bg-[var(--ink)] px-3 py-2 text-[var(--paper)]"
+                    />
+                  </div>
+                  <div>
+                    <label className="block text-[var(--muted)] uppercase mb-1">Chapter</label>
+                    <input
+                      type="text"
+                      placeholder="auto from code if blank"
+                      value={form.chapter}
+                      onChange={(e) => setForm({ ...form, chapter: e.target.value })}
+                      className="w-full rounded border border-[var(--brass)]/30 bg-[var(--ink)] px-3 py-2 text-[var(--paper)]"
+                    />
+                  </div>
+                </div>
+
+                <div>
+                  <label className="block text-[var(--muted)] uppercase mb-1">Description *</label>
+                  <input
+                    type="text"
+                    required
+                    value={form.description}
+                    onChange={(e) => setForm({ ...form, description: e.target.value })}
+                    className="w-full rounded border border-[var(--brass)]/30 bg-[var(--ink)] px-3 py-2 text-[var(--paper)]"
+                  />
+                </div>
+
+                <div className="grid grid-cols-3 gap-3">
+                  <div>
+                    <label className="block text-[var(--muted)] uppercase mb-1">BCD</label>
+                    <input
+                      type="text"
+                      placeholder="e.g. 10%"
+                      value={form.bcd}
+                      onChange={(e) => setForm({ ...form, bcd: e.target.value })}
+                      className="w-full rounded border border-[var(--brass)]/30 bg-[var(--ink)] px-3 py-2 text-[var(--paper)]"
+                    />
+                  </div>
+                  <div>
+                    <label className="block text-[var(--muted)] uppercase mb-1">SWS</label>
+                    <input
+                      type="text"
+                      placeholder="e.g. 10%"
+                      value={form.sws}
+                      onChange={(e) => setForm({ ...form, sws: e.target.value })}
+                      className="w-full rounded border border-[var(--brass)]/30 bg-[var(--ink)] px-3 py-2 text-[var(--paper)]"
+                    />
+                  </div>
+                  <div>
+                    <label className="block text-[var(--muted)] uppercase mb-1">IGST</label>
+                    <input
+                      type="text"
+                      placeholder="e.g. 5%"
+                      value={form.igst}
+                      onChange={(e) => setForm({ ...form, igst: e.target.value })}
+                      className="w-full rounded border border-[var(--brass)]/30 bg-[var(--ink)] px-3 py-2 text-[var(--paper)]"
+                    />
+                  </div>
+                </div>
+
+                <div>
+                  <label className="block text-[var(--muted)] uppercase mb-1">Country</label>
+                  <input
+                    type="text"
+                    value={form.country}
+                    onChange={(e) => setForm({ ...form, country: e.target.value })}
+                    className="w-full rounded border border-[var(--brass)]/30 bg-[var(--ink)] px-3 py-2 text-[var(--paper)]"
+                  />
+                </div>
+
+                <div className="flex justify-end gap-2 border-t border-[var(--brass)]/20 pt-4">
+                  <button
+                    type="button"
+                    onClick={() => setShowModal(false)}
+                    className="rounded border border-[var(--brass)]/30 px-4 py-2 text-[var(--muted)] hover:text-[var(--paper)]"
+                  >
+                    Cancel
+                  </button>
+                  <button type="submit" className="rounded bg-[var(--brass)] px-5 py-2 font-bold text-[var(--ink)]">
+                    {editingId ? "Update HS Code" : "Save HS Code"}
+                  </button>
+                </div>
+              </form>
+            </div>
+          </div>
+        )}
+      </main>
+    </AuthGuard>
+  );
+}
diff --git a/app/admin/page.js b/app/admin/page.js
index 01a68ef..2876dcb 100644
--- a/app/admin/page.js
+++ b/app/admin/page.js
@@ -110,6 +110,22 @@ export default function AdminDashboardPage() {
                   </Link>
                 </div>
 
+                <div className="rounded-xl border border-[var(--brass)]/30 bg-[var(--ink-2)] p-6 shadow-md">
+                  <span className="text-[10px] text-[var(--muted)] uppercase tracking-wider block">Shipment Records</span>
+                  <span className="font-display mt-2 text-3xl text-[var(--paper)] block">{stats?.shipments || 0}</span>
+                  <Link href="/admin/shipments" className="mt-3 inline-block text-[11px] text-[var(--brass)] hover:underline">
+                    Manage Shipments →
+                  </Link>
+                </div>
+
+                <div className="rounded-xl border border-[var(--brass)]/30 bg-[var(--ink-2)] p-6 shadow-md">
+                  <span className="text-[10px] text-[var(--muted)] uppercase tracking-wider block">HS Codes</span>
+                  <span className="font-display mt-2 text-3xl text-[var(--paper)] block">{stats?.hsCodes || 0}</span>
+                  <Link href="/admin/hs-codes" className="mt-3 inline-block text-[11px] text-[var(--brass)] hover:underline">
+                    Manage HS Codes →
+                  </Link>
+                </div>
+
                 <div className="rounded-xl border border-[var(--brass)]/30 bg-[var(--ink-2)] p-6 shadow-md">
                   <span className="text-[10px] text-[var(--muted)] uppercase tracking-wider block">Pending Requests</span>
                   <span className="font-display mt-2 text-3xl text-emerald-400 block">
@@ -162,6 +178,32 @@ export default function AdminDashboardPage() {
                   </p>
                 </Link>
 
+                <Link
+                  href="/admin/shipments"
+                  className="rounded-xl border border-[var(--brass)]/25 bg-[var(--ink-2)] p-6 transition hover:border-[var(--brass)] hover:shadow-xl group"
+                >
+                  <div className="text-2xl">📦</div>
+                  <h3 className="font-display mt-3 text-xl text-[var(--paper)] group-hover:text-[var(--brass)]">
+                    Shipment Records CRUD
+                  </h3>
+                  <p className="mt-2 text-xs text-[var(--muted)] leading-relaxed">
+                    Add, edit, or remove customs shipment/manifest records shown on the trade-data and dashboard pages.
+                  </p>
+                </Link>
+
+                <Link
+                  href="/admin/hs-codes"
+                  className="rounded-xl border border-[var(--brass)]/25 bg-[var(--ink-2)] p-6 transition hover:border-[var(--brass)] hover:shadow-xl group"
+                >
+                  <div className="text-2xl">🏷️</div>
+                  <h3 className="font-display mt-3 text-xl text-[var(--paper)] group-hover:text-[var(--brass)]">
+                    HS Code Directory CRUD
+                  </h3>
+                  <p className="mt-2 text-xs text-[var(--muted)] leading-relaxed">
+                    Manage tariff classification codes, descriptions, and BCD/SWS/IGST duty rates.
+                  </p>
+                </Link>
+
                 <Link
                   href="/admin/requests"
                   className="rounded-xl border border-[var(--brass)]/25 bg-[var(--ink-2)] p-6 transition hover:border-[var(--brass)] hover:shadow-xl group"
diff --git a/app/admin/shipments/page.js b/app/admin/shipments/page.js
new file mode 100644
index 0000000..80d8e33
--- /dev/null
+++ b/app/admin/shipments/page.js
@@ -0,0 +1,374 @@
+"use client";
+
+import { useState, useEffect } from "react";
+import Link from "next/link";
+import AuthGuard from "../../../components/AuthGuard";
+import FieldLabel from "../../../components/FieldLabel";
+
+const emptyForm = {
+  exporter: "",
+  importer: "",
+  product: "",
+  hs_code: "",
+  quantity: "",
+  unit: "MT",
+  shipment_value: "",
+  origin_country: "India",
+  destination_country: "",
+  origin_port: "",
+  destination_port: "",
+  shipment_date: "",
+};
+
+export default function AdminShipmentsPage() {
+  const [shipments, setShipments] = useState([]);
+  const [loading, setLoading] = useState(true);
+  const [error, setError] = useState("");
+  const [showModal, setShowModal] = useState(false);
+  const [editingId, setEditingId] = useState(null);
+  const [form, setForm] = useState(emptyForm);
+
+  useEffect(() => {
+    loadShipments();
+  }, []);
+
+  async function loadShipments() {
+    try {
+      const res = await fetch("/api/admin/shipments", { credentials: "include" });
+      const data = await res.json();
+      if (res.ok) setShipments(data.data || []);
+      else setError(data.error || "Failed to load shipments");
+    } catch (err) {
+      console.error(err);
+      setError("Failed to fetch shipment records");
+    } finally {
+      setLoading(false);
+    }
+  }
+
+  const openCreate = () => {
+    setEditingId(null);
+    setForm(emptyForm);
+    setShowModal(true);
+  };
+
+  const openEdit = (s) => {
+    setEditingId(s.id);
+    setForm({
+      exporter: s.exporter || "",
+      importer: s.importer || "",
+      product: s.product || "",
+      hs_code: s.hs_code || "",
+      quantity: s.quantity ?? "",
+      unit: s.unit || "MT",
+      shipment_value: s.shipment_value ?? "",
+      origin_country: s.origin_country || "",
+      destination_country: s.destination_country || "",
+      origin_port: s.origin_port || "",
+      destination_port: s.destination_port || "",
+      shipment_date: s.shipment_date ? String(s.shipment_date).slice(0, 10) : "",
+    });
+    setShowModal(true);
+  };
+
+  const handleSubmit = async (e) => {
+    e.preventDefault();
+    try {
+      const url = editingId ? `/api/admin/shipments/${editingId}` : "/api/admin/shipments";
+      const method = editingId ? "PUT" : "POST";
+      const res = await fetch(url, {
+        method,
+        headers: { "Content-Type": "application/json" },
+        credentials: "include",
+        body: JSON.stringify(form),
+      });
+      if (res.ok) {
+        setShowModal(false);
+        setForm(emptyForm);
+        setEditingId(null);
+        loadShipments();
+      } else {
+        const data = await res.json().catch(() => ({}));
+        alert(data.error || "Failed to save shipment");
+      }
+    } catch (err) {
+      console.error("Save shipment error", err);
+    }
+  };
+
+  const handleDelete = async (id) => {
+    if (!confirm("Are you sure you want to delete this shipment record?")) return;
+    try {
+      const res = await fetch(`/api/admin/shipments/${id}`, {
+        method: "DELETE",
+        credentials: "include",
+      });
+      if (res.ok) {
+        setShipments((prev) => prev.filter((s) => s.id !== id));
+      }
+    } catch (err) {
+      console.error("Delete shipment error", err);
+    }
+  };
+
+  return (
+    <AuthGuard>
+      <main className="min-h-screen bg-[var(--ink)] py-12 px-6">
+        <div className="mx-auto max-w-6xl">
+          <div className="mb-6">
+            <Link href="/admin" className="font-mono text-xs text-[var(--brass)] hover:underline">
+              ← Back to Admin Console
+            </Link>
+          </div>
+
+          <FieldLabel>Manifest Management</FieldLabel>
+          <div className="flex flex-col md:flex-row md:items-center justify-between gap-4 border-b border-[var(--brass)]/20 pb-6 mb-8">
+            <div>
+              <h1 className="font-display text-3xl md:text-4xl text-[var(--paper)]">
+                Shipment Records CRUD
+              </h1>
+              <p className="mt-1 text-xs text-[var(--muted)] font-mono">
+                Add, edit, or remove customs shipment/manifest records from the live database.
+              </p>
+            </div>
+            <button
+              onClick={openCreate}
+              className="rounded bg-[var(--brass)] px-5 py-2.5 font-mono text-xs font-bold uppercase text-[var(--ink)] hover:brightness-110"
+            >
+              + Add New Shipment
+            </button>
+          </div>
+
+          {loading ? (
+            <div className="flex justify-center py-20 font-mono text-xs text-[var(--brass)]">
+              <span className="h-4 w-4 rounded-full border-2 border-[var(--brass)] border-t-transparent animate-spin mr-2"></span>
+              Loading shipments...
+            </div>
+          ) : error ? (
+            <div className="rounded-xl border border-red-500/30 bg-red-500/10 p-6 text-center text-sm text-red-300 font-mono">
+              {error}
+            </div>
+          ) : (
+            <div className="rounded-xl border border-[var(--brass)]/30 bg-[var(--ink-2)] overflow-hidden shadow-2xl font-mono text-xs">
+              <div className="overflow-x-auto">
+                <table className="w-full text-left border-collapse">
+                  <thead>
+                    <tr className="border-b border-[var(--brass)]/20 bg-[var(--ink)] text-[var(--brass)] uppercase text-[10px] tracking-wider">
+                      <th className="p-4">ID</th>
+                      <th className="p-4">Exporter → Importer</th>
+                      <th className="p-4">Product / HS Code</th>
+                      <th className="p-4">Route</th>
+                      <th className="p-4">Value</th>
+                      <th className="p-4">Date</th>
+                      <th className="p-4 text-right">Actions</th>
+                    </tr>
+                  </thead>
+                  <tbody className="divide-y divide-[var(--brass)]/10 text-[var(--paper)]">
+                    {shipments.length === 0 ? (
+                      <tr>
+                        <td colSpan={7} className="p-6 text-center text-[var(--muted)]">
+                          No shipment records yet.
+                        </td>
+                      </tr>
+                    ) : (
+                      shipments.map((s) => (
+                        <tr key={s.id} className="hover:bg-[var(--brass)]/5 transition">
+                          <td className="p-4 font-bold text-[var(--brass)]">#{s.id}</td>
+                          <td className="p-4">
+                            <p className="font-bold">{s.exporter}</p>
+                            <p className="text-sky-400">→ {s.importer}</p>
+                          </td>
+                          <td className="p-4 max-w-[180px]">
+                            <p className="truncate">{s.product}</p>
+                            <p className="text-[var(--muted)]">HS {s.hs_code || "-"}</p>
+                          </td>
+                          <td className="p-4 text-[var(--muted)]">
+                            <p>{s.origin_port || s.origin_country}</p>
+                            <p className="text-[var(--brass)]">↳ {s.destination_port || s.destination_country}</p>
+                          </td>
+                          <td className="p-4 font-bold text-emerald-400">
+                            {s.shipment_value ? `$${Number(s.shipment_value).toLocaleString()}` : "-"}
+                          </td>
+                          <td className="p-4 text-[var(--muted)]">
+                            {s.shipment_date ? String(s.shipment_date).slice(0, 10) : "-"}
+                          </td>
+                          <td className="p-4 text-right space-x-2 whitespace-nowrap">
+                            <button
+                              onClick={() => openEdit(s)}
+                              className="rounded border border-[var(--brass)]/40 px-3 py-1 text-[10px] text-[var(--brass)] hover:bg-[var(--brass)]/10 font-bold"
+                            >
+                              Edit
+                            </button>
+                            <button
+                              onClick={() => handleDelete(s.id)}
+                              className="rounded border border-red-500/40 px-3 py-1 text-[10px] text-red-400 hover:bg-red-950 font-bold"
+                            >
+                              Delete
+                            </button>
+                          </td>
+                        </tr>
+                      ))
+                    )}
+                  </tbody>
+                </table>
+              </div>
+            </div>
+          )}
+        </div>
+
+        {/* Modal */}
+        {showModal && (
+          <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/80 p-4 font-mono text-xs overflow-y-auto">
+            <div className="w-full max-w-lg rounded-xl border border-[var(--brass)]/40 bg-[var(--ink-2)] p-6 shadow-2xl space-y-4 my-8">
+              <h3 className="font-display text-xl text-[var(--paper)]">
+                {editingId ? "Edit Shipment" : "Add Shipment"}
+              </h3>
+              <form onSubmit={handleSubmit} className="space-y-3">
+                <div className="grid grid-cols-2 gap-3">
+                  <div>
+                    <label className="block text-[var(--muted)] uppercase mb-1">Exporter *</label>
+                    <input
+                      type="text"
+                      required
+                      value={form.exporter}
+                      onChange={(e) => setForm({ ...form, exporter: e.target.value })}
+                      className="w-full rounded border border-[var(--brass)]/30 bg-[var(--ink)] px-3 py-2 text-[var(--paper)]"
+                    />
+                  </div>
+                  <div>
+                    <label className="block text-[var(--muted)] uppercase mb-1">Importer *</label>
+                    <input
+                      type="text"
+                      required
+                      value={form.importer}
+                      onChange={(e) => setForm({ ...form, importer: e.target.value })}
+                      className="w-full rounded border border-[var(--brass)]/30 bg-[var(--ink)] px-3 py-2 text-[var(--paper)]"
+                    />
+                  </div>
+                </div>
+
+                <div>
+                  <label className="block text-[var(--muted)] uppercase mb-1">Product *</label>
+                  <input
+                    type="text"
+                    required
+                    value={form.product}
+                    onChange={(e) => setForm({ ...form, product: e.target.value })}
+                    className="w-full rounded border border-[var(--brass)]/30 bg-[var(--ink)] px-3 py-2 text-[var(--paper)]"
+                  />
+                </div>
+
+                <div className="grid grid-cols-3 gap-3">
+                  <div>
+                    <label className="block text-[var(--muted)] uppercase mb-1">HS Code</label>
+                    <input
+                      type="text"
+                      value={form.hs_code}
+                      onChange={(e) => setForm({ ...form, hs_code: e.target.value })}
+                      className="w-full rounded border border-[var(--brass)]/30 bg-[var(--ink)] px-3 py-2 text-[var(--paper)]"
+                    />
+                  </div>
+                  <div>
+                    <label className="block text-[var(--muted)] uppercase mb-1">Quantity</label>
+                    <input
+                      type="number"
+                      step="0.01"
+                      value={form.quantity}
+                      onChange={(e) => setForm({ ...form, quantity: e.target.value })}
+                      className="w-full rounded border border-[var(--brass)]/30 bg-[var(--ink)] px-3 py-2 text-[var(--paper)]"
+                    />
+                  </div>
+                  <div>
+                    <label className="block text-[var(--muted)] uppercase mb-1">Unit</label>
+                    <input
+                      type="text"
+                      value={form.unit}
+                      onChange={(e) => setForm({ ...form, unit: e.target.value })}
+                      className="w-full rounded border border-[var(--brass)]/30 bg-[var(--ink)] px-3 py-2 text-[var(--paper)]"
+                    />
+                  </div>
+                </div>
+
+                <div>
+                  <label className="block text-[var(--muted)] uppercase mb-1">Shipment Value (USD)</label>
+                  <input
+                    type="number"
+                    step="0.01"
+                    value={form.shipment_value}
+                    onChange={(e) => setForm({ ...form, shipment_value: e.target.value })}
+                    className="w-full rounded border border-[var(--brass)]/30 bg-[var(--ink)] px-3 py-2 text-[var(--paper)]"
+                  />
+                </div>
+
+                <div className="grid grid-cols-2 gap-3">
+                  <div>
+                    <label className="block text-[var(--muted)] uppercase mb-1">Origin Country</label>
+                    <input
+                      type="text"
+                      value={form.origin_country}
+                      onChange={(e) => setForm({ ...form, origin_country: e.target.value })}
+                      className="w-full rounded border border-[var(--brass)]/30 bg-[var(--ink)] px-3 py-2 text-[var(--paper)]"
+                    />
+                  </div>
+                  <div>
+                    <label className="block text-[var(--muted)] uppercase mb-1">Destination Country</label>
+                    <input
+                      type="text"
+                      value={form.destination_country}
+                      onChange={(e) => setForm({ ...form, destination_country: e.target.value })}
+                      className="w-full rounded border border-[var(--brass)]/30 bg-[var(--ink)] px-3 py-2 text-[var(--paper)]"
+                    />
+                  </div>
+                </div>
+
+                <div className="grid grid-cols-2 gap-3">
+                  <div>
+                    <label className="block text-[var(--muted)] uppercase mb-1">Origin Port</label>
+                    <input
+                      type="text"
+                      value={form.origin_port}
+                      onChange={(e) => setForm({ ...form, origin_port: e.target.value })}
+                      className="w-full rounded border border-[var(--brass)]/30 bg-[var(--ink)] px-3 py-2 text-[var(--paper)]"
+                    />
+                  </div>
+                  <div>
+                    <label className="block text-[var(--muted)] uppercase mb-1">Destination Port</label>
+                    <input
+                      type="text"
+                      value={form.destination_port}
+                      onChange={(e) => setForm({ ...form, destination_port: e.target.value })}
+                      className="w-full rounded border border-[var(--brass)]/30 bg-[var(--ink)] px-3 py-2 text-[var(--paper)]"
+                    />
+                  </div>
+                </div>
+
+                <div>
+                  <label className="block text-[var(--muted)] uppercase mb-1">Shipment Date</label>
+                  <input
+                    type="date"
+                    value={form.shipment_date}
+                    onChange={(e) => setForm({ ...form, shipment_date: e.target.value })}
+                    className="w-full rounded border border-[var(--brass)]/30 bg-[var(--ink)] px-3 py-2 text-[var(--paper)]"
+                  />
+                </div>
+
+                <div className="flex justify-end gap-2 border-t border-[var(--brass)]/20 pt-4">
+                  <button
+                    type="button"
+                    onClick={() => setShowModal(false)}
+                    className="rounded border border-[var(--brass)]/30 px-4 py-2 text-[var(--muted)] hover:text-[var(--paper)]"
+                  >
+                    Cancel
+                  </button>
+                  <button type="submit" className="rounded bg-[var(--brass)] px-5 py-2 font-bold text-[var(--ink)]">
+                    {editingId ? "Update Shipment" : "Save Shipment"}
+                  </button>
+                </div>
+              </form>
+            </div>
+          </div>
+        )}
+      </main>
+    </AuthGuard>
+  );
+}
diff --git a/app/api/admin/hs-codes/[id]/route.js b/app/api/admin/hs-codes/[id]/route.js
new file mode 100644
index 0000000..abbc19a
--- /dev/null
+++ b/app/api/admin/hs-codes/[id]/route.js
@@ -0,0 +1,49 @@
+import { query } from "@/lib/db";
+import { requireAdmin } from "@/lib/session";
+
+export async function PUT(request, { params }) {
+  const admin = await requireAdmin();
+  if (admin instanceof Response) return admin;
+
+  const { id } = await params;
+  let body;
+  try {
+    body = await request.json();
+  } catch {
+    return Response.json({ error: "Invalid JSON body" }, { status: 400 });
+  }
+
+  const { code, description, chapter, bcd, sws, igst, country } = body;
+
+  try {
+    await query(
+      `UPDATE hs_codes
+       SET code = ?, description = ?, chapter = ?, bcd = ?, sws = ?, igst = ?, country = ?
+       WHERE id = ?`,
+      [code, description, chapter, bcd, sws, igst, country, Number(id)]
+    );
+
+    return Response.json({ message: "HS code updated" });
+  } catch (err) {
+    if (err && err.code === "ER_DUP_ENTRY") {
+      return Response.json({ error: "This HS code already exists for this country" }, { status: 409 });
+    }
+    console.error("PUT /api/admin/hs-codes/[id] error:", err);
+    return Response.json({ error: "Failed to update HS code" }, { status: 500 });
+  }
+}
+
+export async function DELETE(request, { params }) {
+  const admin = await requireAdmin();
+  if (admin instanceof Response) return admin;
+
+  const { id } = await params;
+
+  try {
+    await query("DELETE FROM hs_codes WHERE id = ?", [Number(id)]);
+    return Response.json({ message: "HS code deleted" });
+  } catch (err) {
+    console.error("DELETE /api/admin/hs-codes/[id] error:", err);
+    return Response.json({ error: "Failed to delete HS code" }, { status: 500 });
+  }
+}
diff --git a/app/api/admin/hs-codes/route.js b/app/api/admin/hs-codes/route.js
new file mode 100644
index 0000000..aa1fcdc
--- /dev/null
+++ b/app/api/admin/hs-codes/route.js
@@ -0,0 +1,56 @@
+import { query } from "@/lib/db";
+import { requireAdmin } from "@/lib/session";
+
+export async function GET(request) {
+  const admin = await requireAdmin();
+  if (admin instanceof Response) return admin;
+
+  try {
+    const rows = await query("SELECT * FROM hs_codes ORDER BY chapter ASC, code ASC LIMIT 300");
+    return Response.json({ data: rows });
+  } catch (err) {
+    console.error("GET /api/admin/hs-codes error:", err);
+    return Response.json({ error: "Failed to load HS codes" }, { status: 500 });
+  }
+}
+
+export async function POST(request) {
+  const admin = await requireAdmin();
+  if (admin instanceof Response) return admin;
+
+  let body;
+  try {
+    body = await request.json();
+  } catch {
+    return Response.json({ error: "Invalid JSON body" }, { status: 400 });
+  }
+
+  const { code, description, chapter, bcd, sws, igst, country } = body;
+  if (!code || !description) {
+    return Response.json({ error: "code and description are required" }, { status: 400 });
+  }
+
+  try {
+    const result = await query(
+      `INSERT INTO hs_codes (code, description, chapter, bcd, sws, igst, country)
+       VALUES (?, ?, ?, ?, ?, ?, ?)`,
+      [
+        code,
+        description,
+        chapter || code.slice(0, 2),
+        bcd || null,
+        sws || null,
+        igst || null,
+        country || "India",
+      ]
+    );
+
+    return Response.json({ id: result.insertId, message: "HS code created" }, { status: 201 });
+  } catch (err) {
+    if (err && err.code === "ER_DUP_ENTRY") {
+      return Response.json({ error: "This HS code already exists for this country" }, { status: 409 });
+    }
+    console.error("POST /api/admin/hs-codes error:", err);
+    return Response.json({ error: "Failed to create HS code" }, { status: 500 });
+  }
+}
diff --git a/app/api/admin/shipments/[id]/route.js b/app/api/admin/shipments/[id]/route.js
new file mode 100644
index 0000000..a3421a9
--- /dev/null
+++ b/app/api/admin/shipments/[id]/route.js
@@ -0,0 +1,75 @@
+import { query } from "@/lib/db";
+import { requireAdmin } from "@/lib/session";
+
+export async function PUT(request, { params }) {
+  const admin = await requireAdmin();
+  if (admin instanceof Response) return admin;
+
+  const { id } = await params;
+  let body;
+  try {
+    body = await request.json();
+  } catch {
+    return Response.json({ error: "Invalid JSON body" }, { status: 400 });
+  }
+
+  const {
+    exporter,
+    importer,
+    product,
+    hs_code,
+    quantity,
+    unit,
+    shipment_value,
+    origin_country,
+    destination_country,
+    origin_port,
+    destination_port,
+    shipment_date,
+  } = body;
+
+  try {
+    await query(
+      `UPDATE shipments
+       SET exporter = ?, importer = ?, product = ?, hs_code = ?, quantity = ?, unit = ?,
+           shipment_value = ?, origin_country = ?, destination_country = ?,
+           origin_port = ?, destination_port = ?, shipment_date = ?
+       WHERE id = ?`,
+      [
+        exporter,
+        importer,
+        product,
+        hs_code,
+        quantity,
+        unit,
+        shipment_value,
+        origin_country,
+        destination_country,
+        origin_port,
+        destination_port,
+        shipment_date,
+        Number(id),
+      ]
+    );
+
+    return Response.json({ message: "Shipment updated" });
+  } catch (err) {
+    console.error("PUT /api/admin/shipments/[id] error:", err);
+    return Response.json({ error: "Failed to update shipment" }, { status: 500 });
+  }
+}
+
+export async function DELETE(request, { params }) {
+  const admin = await requireAdmin();
+  if (admin instanceof Response) return admin;
+
+  const { id } = await params;
+
+  try {
+    await query("DELETE FROM shipments WHERE id = ?", [Number(id)]);
+    return Response.json({ message: "Shipment deleted" });
+  } catch (err) {
+    console.error("DELETE /api/admin/shipments/[id] error:", err);
+    return Response.json({ error: "Failed to delete shipment" }, { status: 500 });
+  }
+}
diff --git a/app/api/admin/shipments/route.js b/app/api/admin/shipments/route.js
new file mode 100644
index 0000000..b269688
--- /dev/null
+++ b/app/api/admin/shipments/route.js
@@ -0,0 +1,74 @@
+import { query } from "@/lib/db";
+import { requireAdmin } from "@/lib/session";
+
+export async function GET(request) {
+  const admin = await requireAdmin();
+  if (admin instanceof Response) return admin;
+
+  try {
+    const rows = await query("SELECT * FROM shipments ORDER BY id DESC LIMIT 200");
+    return Response.json({ data: rows });
+  } catch (err) {
+    console.error("GET /api/admin/shipments error:", err);
+    return Response.json({ error: "Failed to load shipments" }, { status: 500 });
+  }
+}
+
+export async function POST(request) {
+  const admin = await requireAdmin();
+  if (admin instanceof Response) return admin;
+
+  let body;
+  try {
+    body = await request.json();
+  } catch {
+    return Response.json({ error: "Invalid JSON body" }, { status: 400 });
+  }
+
+  const {
+    exporter,
+    importer,
+    product,
+    hs_code,
+    quantity,
+    unit,
+    shipment_value,
+    origin_country,
+    destination_country,
+    origin_port,
+    destination_port,
+    shipment_date,
+  } = body;
+
+  if (!exporter || !importer || !product) {
+    return Response.json({ error: "exporter, importer, and product are required" }, { status: 400 });
+  }
+
+  try {
+    const result = await query(
+      `INSERT INTO shipments
+        (exporter, importer, product, hs_code, quantity, unit, shipment_value,
+         origin_country, destination_country, origin_port, destination_port, shipment_date)
+       VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)`,
+      [
+        exporter,
+        importer,
+        product,
+        hs_code || null,
+        quantity || null,
+        unit || null,
+        shipment_value || null,
+        origin_country || null,
+        destination_country || null,
+        origin_port || null,
+        destination_port || null,
+        shipment_date || null,
+      ]
+    );
+
+    return Response.json({ id: result.insertId, message: "Shipment created" }, { status: 201 });
+  } catch (err) {
+    console.error("POST /api/admin/shipments error:", err);
+    return Response.json({ error: "Failed to create shipment" }, { status: 500 });
+  }
+}